{
	"comment": "This is the config file for random agent for the continuing cartpole environment.",
	"agent-name": "random",
	"environment-name": "cartpole",
	"agent-settings": {
		"enable-debug": true,
		"seed": 25
	},
	"environment-settings": {
		"seed": 15
	},
	"experiment-settings": {
		"steps": 100000,
		"data-path": "data/random",
		"should-log-traces": false,
		"should-log-episode-lengths": true,
		"debug-interval": 1000
	}
}
